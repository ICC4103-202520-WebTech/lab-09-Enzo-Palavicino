<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Lab 6" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="stylesheet" href="https://unpkg.com/trix@2.1.4/dist/trix.css">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "actiontext", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <%= link_to "Recipe Book", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="mainNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item"><%= link_to "All Recipes", recipes_path, class: "nav-link" %></li>
            <% if policy(Recipe).new? %>
              <li class="nav-item"><%= link_to "New Recipe", new_recipe_path, class: "nav-link" %></li>
            <% end %>
          </ul>
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <% if user_signed_in? %>
              <li class="nav-item d-flex align-items-center text-white-50 px-2">
                <i class="bi bi-person-circle me-2"></i>
                <span class="small"><%= current_user.email %></span>
                <% if current_user.admin? %>
                  <span class="badge rounded-pill text-bg-warning text-dark ms-2">Admin</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= button_to "Log out", destroy_user_session_path,
                              method: :delete,
                              data: { turbo_confirm: "Are you sure you want to log out?" },
                              class: "nav-link btn btn-link px-0 text-decoration-none",
                              form: { class: "d-inline" } %>
              </li>
            <% else %>
              <li class="nav-item"><%= link_to "Sign up", new_user_registration_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "Log in", new_user_session_path, class: "nav-link" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <main class="flex-grow-1 py-4">
      <div class="container">
        <% flash.each do |key, value| %>
          <% next if value.blank? %>
          <% bootstrap_class = {
                "notice" => "alert-success",
                "alert" => "alert-danger",
                "error" => "alert-danger"
              }[key.to_s] || "alert-info" %>
          <div class="alert <%= bootstrap_class %> alert-dismissible fade show" role="alert">
            <%= value %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </main>

    <footer class="bg-dark text-white-50 py-3 mt-auto">
      <div class="container small">
        <span>&copy; <%= Time.current.year %> Recipe Book</span>
      </div>
    </footer>
  </body>
</html>
